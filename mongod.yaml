systemLog:
        destination: file
        path: "$HOME/logmongod.log"
        logAppend: true
storage:
        journal:
                enabled: true
        dbPath: "$HOME/datamongo/"
processManagement:
        fork: true
